# Hugging Face model id (3B, dim 2048). Changing this requires a vec_chunks rebuild on MCP.
MODEL_ID=ai-sage/Giga-Embeddings-instruct
PORT=7997

# Optional. The instruct model is not gated; set only if your HF account needs it.
HF_TOKEN=
